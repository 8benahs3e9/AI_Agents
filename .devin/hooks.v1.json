{
  "SessionStart": [
    {
      "hooks": [
        {
          "type": "prompt",
          "prompt": "🛡️ Runbook Toolkit Enforcer Active - Working in AI_Agents directory - runbook toolkit instructions will be applied automatically. Toolkit location: /home/user/AI_Agents/runbook_toolkit/"
        }
      ]
    }
  ],
  "PreToolUse": [
    {
      "matcher": "exec",
      "hooks": [
        {
          "type": "prompt",
          "prompt": "Check if this command should use the runbook toolkit instead. If this is a system modification (rm, apt, systemctl, chmod, chown, iptables, etc.), use the runbook toolkit workflow: 1) Create PLAN.json in /tmp/artifacts/, 2) Execute with python -m runbook_toolkit execute --plan /tmp/artifacts/PLAN.json --artifacts-dir /tmp/artifacts, 3) Verify results. Only use direct exec for read-only operations."
        }
      ]
    }
  ]
}